# Mock Roleplaying Backend Consumer

A clean framework-neutral consumer that creates and restores a PomegranateUI layout while keeping its roleplaying backend record as a separate host-owned object. It imports package roots only and remains independent of any production host backend.
