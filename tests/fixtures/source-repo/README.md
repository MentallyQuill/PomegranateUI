# Importer Fixture Shape

`tests/unit/extraction.test.mjs` creates a real temporary Git repository with this logical shape so commit reads, binary preservation, dirty-working-tree isolation, mapping validation, and referenced-asset handling exercise Git itself rather than a mock.
