# Sonder Baseline

The commit-addressed importer populates this directory with byte-for-byte legacy evidence. These artifacts are behavioral and visual oracles, not production PomegranateUI packages.
